default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: hotel_crm_development

test:
  <<: *default
  database: hotel_crm_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
